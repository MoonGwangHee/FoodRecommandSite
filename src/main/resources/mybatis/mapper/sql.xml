<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Project.page.domain.mapper.MemberMapper">
    <insert id="saveUser" parameterMap="Project.page.Users.UserVo">
        INSERT INTO mproject.user_table
        (id, email, number, username, address, age, password)
        VALUES(#{id},#{email},#[number},#{username},#{address},#{age},#{password});
    </insert>
</mapper>