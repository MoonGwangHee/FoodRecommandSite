<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Project.page.global.dao.UserMapper">
    <select id="getUserList" resultType="Project.page.global.dto.user">
        SELECT * FROM mproject.user_table;
    </select>
</mapper>